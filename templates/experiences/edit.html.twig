{% extends 'base_admin.html.twig' %}

{% block title %}Edit Experiences{% endblock %}

{% block body %}
    <h1>Modifier {{experience.title}}</h1>

    {{ include('experiences/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="right-align">
	    <a href="{{ path('experiences_index') }}" class="btn"><i class="material-icons back">arrow_back</i>Retour</a>
	    {{ include('experiences/_delete_form.html.twig') }}
	</div>
{% endblock %}
