{% extends 'base_admin.html.twig' %}

{% block title %}Edit Formation{% endblock %}

{% block body %}
    <h1>Edit {{formation.name}}</h1>

    {{ include('formation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="right-align">
	    <a href="{{ path('formation_index') }}" class="btn"><i class="material-icons back">arrow_back</i>Retour</a>
	    {{ include('formation/_delete_form.html.twig') }}
	</div>
{% endblock %}
