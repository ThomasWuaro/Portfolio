{% extends 'base_admin.html.twig' %}

{% block title %}Edit Skill{% endblock %}

{% block body %}
    <h1>Modifier {{skill.name}}</h1>

    {{ include('skill/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="right-align">
	    <a href="{{ path('skill_index') }}" class="btn"><i class="material-icons back">arrow_back</i>Retour</a>
	    {{ include('skill/_delete_form.html.twig') }}
	</div>

{% endblock %}
